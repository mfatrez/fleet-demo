namespace: cert-manager
helm:
  chart: 
  repo: "https://charts.jetstack.io"
  version: 1.19.1
  values:
    crds:
      enabled: true
  createNamespace: true
overrideTargets:
  - clusterSelector:
      matchLabels:
        infra: workload
