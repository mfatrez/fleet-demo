defaultNamespace: default
namespace: cert-manager
helm:
  chart: ./chart
  repo: "https://charts.jetstack.io/cert-manager"
  version: v1.19.1
  values:
    crds:
      enabled: true
  releaseName: cert-manager-fleet
overrideTargets:
  - clusterSelector:
      matchLabels:
        infra: workload
