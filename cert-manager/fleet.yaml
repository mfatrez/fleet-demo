namespace: cert-manager
helm:
  releaseName: cert-manager
  chart: 
    repo: "https://charts.jetstack.io"
    name: cert-manager
    version: "1.19.1"
  values:
    crds:
      enabled: true
  createNamespace: true
targets:
  - name: default
    clusterSelector:
      matchLabels:
        infra: workload
