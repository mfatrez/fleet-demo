defaultNamespace: metallb-system
helm:
  chart: "oci://dp.apps.rancher.io/charts/metallb"
  version: 0.15.2
  values:
    prometheus:
      metricsPort: 7772
  releaseName: metallb-fleet
overrideTargets:
  - clusterSelector:
      matchLabels:
        infra: workload
